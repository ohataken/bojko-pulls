name: Deploy bojko_pulls_session_destroy

on:
  push:
    branches:
      - main
    paths:
      - 'bojko_pulls_session_destroy/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          bundler-cache: false
      - run: cd bojko_pulls_session_destroy && bundle config set --local path 'vendor/bundle' && bundle install
      - run: cd bojko_pulls_session_destroy && zip -r ../bojko_pulls_session_destroy.zip .
